---
model: haiku
---

# crack — recover plaintext from a hash or KeePass file (T1110.002, CWE-916)

Turn a captured hash into a usable password with John the Ripper and a wordlist — the MD5s a SQL dump
yields, or the master password of a downloaded KeePass/Office/zip file. Exploitation tier — the ROE
must allow-list T1110.002 and name an authoriser. Take hashes from `searu loot --reveal`.

Run — for a hash, the target is only for scope (the cracking is offline); for a downloaded file, point
the **target** at it with `src:<workspace-relative-path>` (that is what mounts the file for John):

    searu run crack --technique T1110.002 --target http://host:port -- \
      --hash <hash> --format raw-md5 --wordlist seclists:Passwords/Common-Credentials/Pwdb_top-10000.txt
    searu run crack --technique T1110.002 --target src:pentest/outputs/fetch/0001/secrets.kdbx -- \
      --wordlist seclists:Passwords/Common-Credentials/Pwdb_top-10000.txt

`--format` is a John format (`raw-md5`, `raw-sha256`, `bcrypt`, …); a `src:` file is converted
(keepass2john/zip2john) and auto-detected — crack hands it to John for you. When a plain wordlist misses
(a KeePass master, a mangled password), mangle it with a rule set via `--rules`:

    searu run crack --technique T1110.002 --target src:secrets.kdbx -- \
      --wordlist seclists:Passwords/Common-Credentials/Pwdb_top-10000.txt --rules best64

If keepass2john can't parse the file it now says so plainly (rather than silently loading nothing).
Note: full `rockyou.txt` ships in SecLists only as `.tar.gz` (the `seclists:` fetch 404s on the raw
`.txt`), so use `Pwdb_top-10000.txt` + `--rules` (best64/jumbo) for reach. Read results:

    searu loot --reveal            # the recovered plaintext (category password)
    searu findings --tool crack    # a confirmed CWE-916 finding

Then log in with the recovered credential, re-crawl the authenticated surface (`browser --login`) and
re-run `searu coverage`. Don't paste raw John output. See `searu tool advice crack`.
