Sife deployment key
===================

id_rsa is the shared deploy key for pushing releases to the production
fleet (deploy@sife.io). It is passphrase-protected; the passphrase is the
standard ops secret recorded in the team vault.

Do not commit this directory to the application repository.
