# The previous Python toolkit, kept as a read-only reference. Never committed.
# TODO: remove this once replacement complete
/old-version/

# IDE metadata.
/.idea/

# Rust build output.
/target/

# Per-engagement working data (findings/loot/ROE) — never committed.
/pentest/

# Local lab targets cloned for testing (e.g. the CWE-78 app).
/examples/CWE-78/
